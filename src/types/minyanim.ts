export interface typeOfDate{
    value: number
    label: string
}

export interface LineItemTable {
    id: string;
    blink: string;
    startTime: number;
    endTime: number;
    room: number;
  }
  